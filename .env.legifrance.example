# ==========================================
# Légifrance LEGI - Configuration
# ==========================================
# Copy this file to .env and configure for your environment

# Serveur SSH
LEGI_SSH_HOST=user@your-server-ip
LEGI_SSH_KEY=  # Empty = use default SSH key (~/.ssh/id_rsa or id_ed25519)
LEGI_CLI_PATH=/path/to/legifrance/scripts/legi_cli.py  # Path to CLI script on remote server
# Alternative: can use global symlink
# LEGI_CLI_PATH=/usr/local/bin/legi-cli

# Timeouts (seconds)
LEGI_TIMEOUT_SUMMARY=60   # get_summary operation (2.7s with precalc cache for depth=5!)
LEGI_TIMEOUT_ARTICLE=30   # get_article operation (fast)

# Logging
LEGI_LOG_LEVEL=INFO  # DEBUG, INFO, WARNING, ERROR

# ==========================================
# Performance Notes:
# - Cache précalculé: depth=5 en 2.7s au lieu d'1h sans cache
# - Cache location: /root/cache/legi/*.json.gz (~150 MB)
# - 15 fichiers précalculés (3 scopes × 5 depths)
# ==========================================

# ==========================================
# Setup Instructions:
# 1. SSH key authentication must be configured:
#    ssh-copy-id user@your-server-ip
#
# 2. Test connection:
#    ssh user@your-server-ip "echo OK"
#
# 3. Test CLI (local on server):
#    ssh user@your-server-ip "legi-cli get_codes --limit=1"
#
# 4. Test from MCP:
#    curl -X POST http://localhost:8000/execute \
#      -H "Content-Type: application/json" \
#      -d '{"tool":"legifrance_legi","params":{"operation":"get_summary","depth":1,"limit":3}}'
# ==========================================
