








// Workers Process - Overlay Orchestrator (split across core/events)
(function(){
  // Keep for backward-compat include order. This file orchestrates the overlay by delegating
  // to workers-process-overlay-core.js (DOM) and workers-process-overlay-events.js (bindings). 
  // No logic here to keep size well below 7KB.
})();

 
 
 
 
 
 
