# Git ignore
.env
.dgy_backup/
sqlite3/
__pycache__/
**/__pycache__/
*.pyc
src/add_mcp_server.egg-info/

# OS junk
.DS_Store

# Local data and generated content (not source)
docs/
files/
script_executor/

# Virtual environments
venv/
.venv/

# IMAP tool data (credentials, attachments, cache)
files/imap/

*.egg-info/
*.egg-info

# Skyfield ephemeris data (downloaded on first use)
*.bsp
*.npy

# SSH Keys and local scripts (CRITICAL: never commit)
ssh_keys/
scripts_ssh/

# Project-local SQLite DBs (safety)
*.db
!sqlite3/*.db

# Additional hardening (src-local secrets/scripts)
src/ssh_keys/
src/scripts_ssh/
