
# 🔍 MÉTHODOLOGIE AUDIT TOOLS

## État: 21/40 audités ✅

**Tools restants (19)**: astronomy, coingecko, excel_to_sqlite, ffmpeg_frames, flight_tracker, git, imap, math, news_aggregator, office_to_pdf, ollama_local, open_meteo, pdf2text, pdf_download, reddit_intelligence, script_executor, ssh_admin, universal_doc_scraper, youtube_search

**✅ DERNIER AUDIT: device_location** (21/40)
- Score: 9.6/10 ⭐⭐⭐⭐
- Tests: 3/3 validation OK ; NR 3/5 OK (2 à faire en QA)
- Status: COMPLET et validé (commit local), push après QA NR

---

## WORKSPACE

⚠️ Auto-sync: writeFile/deleteFile mettent à jour le workspace automatiquement (pas de reload)
⚠️ Pas de double-load: vérifier qu'un fichier n'est pas déjà chargé
💡 Limite: 500 KB total, unload systématiquement à la fin

---

## PROCESS (10 étapes)

### 1. SÉLECTION
`random pick_random` sur la liste des tools restants

### 2. CHARGEMENT
```
load_file: ["src/tools/{tool}.py", "src/tool_specs/{tool}.json", "src/tools/_{tool}/*.py"]
```

### 3. TESTS PRÉLIMINAIRES

**⚠️ OBLIGATOIRE - UN PAR UN (jamais parallèle)**
**⚠️ PAS DE `/tools?reload=1` (119KB, floods LLM)**

A. Charger JSON spec localement (si pas déjà chargé)
B. 🔴 TESTS RÉELS OBLIGATOIRES (minimum 3 tests baseline):
   - Test 1: Opération la plus simple
   - Test 2: Opération avec params
   - Test 3: Vérification résultat (si applicable)
C. 🚨 SI TESTS IMPOSSIBLES (pas de credentials/accès):
   - ABORT AUDIT immédiatement
   - Ne PAS continuer l'audit sans tests
   - Documenter la raison dans procédure
   - Passer au tool suivant
D. Observations: outputs, erreurs, validation, truncation, counts, timeouts

### 4. AUDIT

**JSON Spec**:
- [x] category, displayName, additionalProperties:false
- [x] Arrays avec items, enums complets
- [x] required corrects, defaults explicites
- [x] Descriptions claires (1 ligne max)

**Code**:
- [x] Output size: limits + truncation warnings (N/A listes)
- [x] Validation stricte (ajoutée)
- [x] Try-catch global (ajouté en API)
- [x] Timeouts cohérents
- [x] Logging (INFO/WARNING/ERROR)
- [x] Pas side-effects import
- [x] Fichiers < 7KB

**Notation** (/10):
JSON Spec | Architecture | Sécurité | Robustesse | Conformité | Performance | Maintenabilité | Documentation

### 5. CORRECTIFS

Format commit:
```
fix({tool}): critical audit fixes (X.X→Y.Y/10)

🔴 CRITICAL: [bugs tests]
🟠 MAJOR: [outputs, validation]
🟢 IMPROVEMENTS: [truncation, logging]

TECHNICAL: conformité XX%→YY%
TESTS: X/X non-régression OK
SCORE: X.X→Y.Y/10
```

### 6. TESTS VALIDATION

🔴 OBLIGATOIRE APRÈS CORRECTIFS

Re-tester TOUS les tests préliminaires + nouveaux cas si nécessaire

### 7. TESTS NON-RÉGRESSION

⚠️ OBLIGATOIRE - UN PAR UN

A. Identifier toutes fonctionnalités
B. Tester exhaustivement (minimum 5 tests)
C. Tableau synthèse OBLIGATOIRE
D. Si régression: STOP → corriger → reprendre étape 6
E. Validation finale: tous tests ✅

**🚨 ABORT SI:**
- Régression détectée après correctif
- Impossible de tester (pas de credentials)
- Tests échouent de manière irrémédiable

### 8. CHANGELOG

Ajouter dans `[Unreleased]`:
```markdown
### {tool} - [DATE] ✅ X.X→Y.Y/10 ⭐

**Fixed**: [bugs]
**Added**: [features]
**Technical**: conformité XX%→YY%
**Tests**: X/X OK
**SCORE FINAL: Y.Y/10**
**Known Issues**: [si applicable]
```

### 9. COMMIT + PUSH

🚨 COMMIT SEULEMENT SI:
- ✅ Tous tests validation OK
- ✅ Tests non-régression OK
- ✅ Pas de régression détectée

```
git commit_all -m "fix({tool}): critical audit fixes (X.X→Y.Y/10) ..."
git push main
```

🚨 EN CAS D'ERREUR APRÈS COMMIT:
- Rollback si possible
- Sinon: commit correctif immédiat
- MAJ changelog avec correction

### 10. UNLOAD + MAJ PROCÉDURE

```
unload_file: ["src/tools/{tool}.py", ...]
```

**MAJ procédure**:
- Incrémenter compteur (20→21/40)
- Retirer de liste tools restants
- Commit: `docs(audit): update methodology - {tool} audited (X/40)`
