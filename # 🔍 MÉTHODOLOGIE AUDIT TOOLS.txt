# 🔍 MÉTHODOLOGIE AUDIT TOOLS

## État: 4/40 audités
- discord_bot: 9.6/10 ✅
- ship_tracker: 9.4/10 ✅  
- random: créé ✅
- chess_com: 8.8/10 ✅

---

## PROCESS (9 étapes)

### 1. SÉLECTION RANDOM
- Utiliser le tool `random`: `pick_random` avec la liste des 36 tools restants
- Simple et rapide

### 2. CHARGEMENT
```
load_file: [
  "src/tools/{tool}.py",
  "src/tool_specs/{tool}.json", 
  "src/tools/_{tool}/*.py"  // TOUS
]
```

### 3. AUDIT

#### A. JSON SPEC (CRITIQUE - LLM)
- [ ] `category` définie
- [ ] `displayName` présent
- [ ] `additionalProperties: false`
- [ ] Arrays avec `items`
- [ ] Enums complets
- [ ] `description` claire (1 ligne max)
- [ ] `required` corrects
- [ ] Defaults sensés
- [ ] Limits min/max

**Questions**:
- LLM comprend CHAQUE param ?
- Descriptions ambiguës ?
- Params secrets ?

#### B. CODE
- [ ] Output size: limits + truncation warnings
- [ ] Validation stricte
- [ ] Try-catch global
- [ ] Timeouts
- [ ] Pas side-effects import
- [ ] Logging minimal
- [ ] DRY
- [ ] Modulaire

**Outputs MINIMAUX**:
```python
✅ return [1,2,3]
✅ return "heads"

❌ return {"success": true, "operation": "...", ...}
```

#### C. NOTATION
- JSON Spec LLM: /10
- Architecture: /10
- Sécurité: /10
- Robustesse: /10
- Conformité: /10
- Performance: /10
- Maintenabilité: /10
- Documentation: /10

**Score total**: X.X/10

### 4. CORRECTIFS
```
fix({tool}): critical audit fixes (X.X→Y.Y/10)

🔴 CRITICAL:
- Bug 1
  → BEFORE: ...
  → AFTER: ...

🟡 MAJOR:
- Amélioration 1

🟢 IMPROVEMENTS:
- Amélioration 2

TECHNICAL DETAILS:
- file.py: +XXX bytes
- Conformité: XX% → YY%

AUDIT SCORE: X.X/10 → Y.Y/10 ⭐⭐⭐⭐

Part of: [Unreleased] Tools Audit Campaign
```

### 5. TEST HTTP ⚡ AUTOMATIQUE

**IMPORTANT**: Tester directement avec `http_client` pour valider les correctifs.

#### A. Reload tools
```bash
GET /tools?reload=1
```

#### B. Tester 2-3 opérations représentatives
Choisir des opérations qui couvrent :
- Une opération simple (ex: get_info, list, search)
- Une opération avec paramètres (ex: search avec query)
- Une opération avec limits (si applicable)

**Template de test**:
```python
# Test 1: Opération simple
POST /execute {
  "tool": "{tool}",
  "params": {
    "operation": "xxx",
    ...
  }
}

# Test 2: Opération avec params
POST /execute {
  "tool": "{tool}",
  "params": {
    "operation": "yyy",
    "query": "test",
    "limit": 5
  }
}

# Test 3: Opération edge case (optionnel)
POST /execute {
  "tool": "{tool}",
  "params": {
    "operation": "zzz",
    ...
  }
}
```

#### C. Validation des résultats
- ✅ Extraire **JUSTE** `body.result` (pas le HTTP complet)
- ✅ Vérifier structure conforme
- ✅ Vérifier truncation warnings (si applicable)
- ✅ Vérifier counts clairs (total vs returned)
- ✅ Pas de metadata superflue

**Format d'affichage**:
```
✅ Test 1 PASSED: {operation} fonctionne
Result: {extraire les 5-10 premières lignes du result}

❌ Test 2 FAILED: {operation} erreur
Error: {message d'erreur}
```

#### D. En cas d'erreur
- Analyser l'erreur
- Corriger le code
- Re-tester
- Documenter dans CHANGELOG (section "Fixed during testing")

### 6. CHANGELOG

Ajouter section dans `[Unreleased]`:
```markdown
### {tool} - [DATE] ✅ AUDITED

**Score**: X.X → Y.Y/10

#### Fixed
- Bug 1
- Bug 2

#### Improved
- Amélioration 1

#### Technical Details
- Files modifiés
- Conformité: XX% → YY%

#### Audit Results
[Tableau scores]

**SCORE FINAL: Y.Y/10** ⭐⭐⭐⭐⭐

#### Known Issues (si applicable)
- Issue 1: Description (confirmé Perplexity si externe)
```

**IMPORTANT**: Vérifier la taille du CHANGELOG :
- Si > 10KB : **ROTATION OBLIGATOIRE**
  - Créer `changelogs/CHANGELOG_{start}_to_{end}.md`
  - Garder uniquement `[Unreleased]` dans CHANGELOG.md racine
  - Note en tête: "Older entries have been archived under changelogs/"

### 7. COMMIT + PUSH
```bash
git commit_all -m "fix({tool}): critical audit fixes (X.X→Y.Y/10)

🔴 CRITICAL:
- Fix 1

🟡 MAJOR:
- Fix 2

🟢 IMPROVEMENTS:
- Fix 3

TECHNICAL DETAILS:
- file.py: +XXX bytes
- Conformité: XX% → YY%

AUDIT SCORE: X.X/10 → Y.Y/10 ⭐⭐⭐⭐

KNOWN ISSUES (si applicable):
- Issue 1

Part of: [Unreleased] Tools Audit Campaign

CHANGELOG: [OK / Rotated to changelogs/XXX]"

git push main
```

### 8. UNLOAD WORKSPACE 🧹

**CRITIQUE**: Libérer l'espace workspace pour le prochain audit.

```bash
unload_file: [
  "src/tools/{tool}.py",
  "src/tool_specs/{tool}.json",
  "src/tools/_{tool}/*.py"  // TOUS les fichiers chargés
]
```

**Vérification**:
- Confirmer l'espace libéré (workspace summary)
- Garder uniquement les fichiers de base (LLM_DEV_GUIDE.md, README.md, CHANGELOG.md, procédure)

### 9. METTRE À JOUR CETTE PROCÉDURE 📝

**OBLIGATOIRE**: Après chaque audit complet, mettre à jour cette procédure :

1. **Incrémenter le compteur**
   - "État: 4/40 audités" → "État: 5/40 audités"

2. **Ajouter le tool à la liste des audités**
   ```
   - discord_bot: 9.6/10 ✅
   - ship_tracker: 9.4/10 ✅  
   - random: créé ✅
   - chess_com: 8.8/10 ✅
   - {nouveau_tool}: X.X/10 ✅  ← AJOUTER ICI
   ```

3. **Retirer le tool de la liste des restants**
   - Liste "36 tools restants" → "35 tools restants"
   - Supprimer `{nouveau_tool}` de la liste alphabétique

4. **Commit la procédure**
   ```bash
   git commit -m "docs(audit): update methodology - {tool} audited (X/40)"
   git push main
   ```

**Exemple**:
```
# AVANT
État: 4/40 audités
36 tools restants: academic_research_super, astronomy, ...

# APRÈS (audit de astronomy)
État: 5/40 audités
- astronomy: 8.5/10 ✅
35 tools restants: academic_research_super, aviation_weather, ...
```

---

## RÈGLES ABSOLUES

1. **JSON**: Auditer lisibilité LLM
2. **OUTPUTS**: Minimaux (pas metadata)
3. **TRUNCATION**: Warnings si > 50 items
4. **TESTS**: ⚡ **AUTOMATIQUES** avec http_client (extraire `body.result` uniquement)
5. **CHANGELOG**: Toujours màj + rotation si > 10KB
6. **PAS RELEASE**: Juste push
7. **PERPLEXITY**: Systématique pour les 404 ou bugs suspects d'API externe
8. **UNLOAD**: 🧹 **TOUJOURS** unload à la fin pour libérer workspace
9. **PROCÉDURE**: 📝 **TOUJOURS** mettre à jour cette procédure après chaque audit

---

## COMMANDE REPRISE

```
Audite le prochain tool (random)
```

**Workspace déjà chargé**: LLM_DEV_GUIDE.md, README.md, CHANGELOG.md, procédure

**36 tools restants**: academic_research_super, astronomy, aviation_weather, call_llm, coingecko, date, device_location, discord_webhook, email_send, excel_to_sqlite, ffmpeg_frames, flight_tracker, generate_edit_image, git, gitbook, google_maps, http_client, imap, math, news_aggregator, office_to_pdf, ollama_local, open_meteo, pdf2text, pdf_download, pdf_search, reddit_intelligence, script_executor, sqlite_db, ssh_admin, telegram_bot, trivia_api, universal_doc_scraper, velib, video_transcribe, youtube_download, youtube_search

Prêt. 🚀
