# 🔍 MÉTHODOLOGIE AUDIT TOOLS

## État: 18/40 audités

**Tools restants (22)**: astronomy, coingecko, device_location, discord_webhook, excel_to_sqlite, ffmpeg_frames, flight_tracker, git, imap, math, news_aggregator, office_to_pdf, ollama_local, open_meteo, pdf2text, pdf_download, reddit_intelligence, script_executor, ssh_admin, universal_doc_scraper, video_transcribe, youtube_search

---

## WORKSPACE

⚠️ **Auto-sync**: writeFile/deleteFile mettent à jour le workspace automatiquement (pas de reload)
⚠️ **Pas de double-load**: vérifier qu'un fichier n'est pas déjà chargé
💡 **Limite**: 500 KB total, unload systématiquement à la fin

---

## PROCESS (10 étapes)

### 1. SÉLECTION
`random pick_random` sur la liste des tools restants

### 2. CHARGEMENT
```
load_file: ["src/tools/{tool}.py", "src/tool_specs/{tool}.json", "src/tools/_{tool}/*.py"]
```

### 3. TESTS PRÉLIMINAIRES

**⚠️ UN PAR UN (jamais parallèle)**
**⚠️ PAS DE `/tools?reload=1` (119KB, floods LLM)**

A. Charger JSON spec localement (si pas déjà chargé)
B. 5 tests baseline: simple, params, validation, edge case, complexe
C. Observations: outputs, erreurs, validation, truncation, counts, timeouts

### 4. AUDIT

**JSON Spec**:
- [ ] category, displayName, additionalProperties:false
- [ ] Arrays avec items, enums complets
- [ ] required corrects, defaults explicites
- [ ] Descriptions claires (1 ligne max)

**Code**:
- [ ] Output size: limits + truncation warnings
- [ ] Validation stricte
- [ ] Try-catch global
- [ ] Timeouts cohérents
- [ ] Logging (INFO/WARNING/ERROR)
- [ ] Pas side-effects import
- [ ] Fichiers < 7KB

**Notation** (/10):
JSON Spec | Architecture | Sécurité | Robustesse | Conformité | Performance | Maintenabilité | Documentation

### 5. CORRECTIFS

**⚠️ DÉCOUPAGE**: Si fichier > 7KB, découper logiquement

Format commit:
```
fix({tool}): critical audit fixes (X.X→Y.Y/10)

🔴 CRITICAL: [bugs tests]
🟡 MAJOR: [outputs, validation]
🟢 IMPROVEMENTS: [truncation, logging]

TECHNICAL: conformité XX%→YY%
TESTS: X/X non-régression OK
SCORE: X.X→Y.Y/10
```

### 6. TESTS VALIDATION

Re-tester uniquement ce qui posait problème

### 7. TESTS NON-RÉGRESSION

**⚠️ OBLIGATOIRE - UN PAR UN**

A. Identifier toutes fonctionnalités
B. Tester exhaustivement
C. Tableau synthèse OBLIGATOIRE
D. Si régression: STOP → corriger → reprendre
E. Validation finale: tous tests ✅

### 8. CHANGELOG

Ajouter dans `[Unreleased]`:
```markdown
### {tool} - [DATE] ✅ X.X→Y.Y/10 ⭐

**Fixed**: [bugs]
**Added**: [features]
**Technical**: conformité XX%→YY%
**Tests**: X/X OK
**SCORE FINAL: Y.Y/10**
**Known Issues**: [si applicable]
```

### 9. COMMIT + PUSH

```
git commit_all -m "fix({tool}): critical audit fixes (X.X→Y.Y/10) ..."
git push main
```

### 10. UNLOAD + MAJ PROCÉDURE

```
unload_file: ["src/tools/{tool}.py", ...]
```

**MAJ procédure**:
- Incrémenter compteur (17→18/40)
- Retirer de liste tools restants
- Commit: `docs(audit): update methodology - {tool} audited (X/40)`

---

## RÈGLES ABSOLUES

1. 🚨 Appels UN PAR UN (jamais parallèle)
2. 🚨 PAS DE `/tools?reload=1` (floods LLM)
3. Charger JSON spec localement avant tests
4. Tests avant audit (étape 3)
5. Outputs minimaux (pas metadata success/operation côté tool)
6. Truncation warnings si > 50 items
7. Tests validation après correctifs
8. 🚨 Tests non-régression OBLIGATOIRES
9. CHANGELOG màj + rotation si > 10KB
10. Unload toujours à la fin
11. Procédure màj après chaque audit
12. 🚨 Découpage fichiers si > 7KB
13. 🚨 CHANGELOG condensé (essentiel uniquement)
14. 🚨 Workspace auto-sync
15. 🚨 Tester les correctifs avant commit

---

## COMMANDE REPRISE

```
Audite le prochain tool (random)
```

Workspace permanent: LLM_DEV_GUIDE.md, README.md, CHANGELOG.md, src/tools/README.md, procédure
