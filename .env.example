# Dragonfly MCP Server — Example environment file
# Copy to .env and adjust values.

# Logging
LOG_LEVEL=INFO

# MCP server
MCP_HOST=127.0.0.1
MCP_PORT=8000

# OpenAI (Sora Video API)
OPENAI_API_KEY=
OPENAI_API_BASE=https://api.openai.com/v1

# Kling API
# Option A: JWT (recommended) — generate Bearer automatically from these two keys
KLING_ACCESS_KEY=
KLING_SECRET_KEY=
# Option B: Legacy token (if provided by your account). If set, used as Bearer directly.
KLING_API_KEY=
KLING_API_BASE=https://api-singapore.klingai.com
